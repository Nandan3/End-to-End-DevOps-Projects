1. create EC2 Instance:
    type: t2.large
    storage: 30GB

    
    
2. $sudo usermod -aG docker ubuntu           # adding ubuntu user to the docker group
  

3. Resize File system: (if no space left to run application issue):
	1. Increase the instance volume to 30GB
	
	

  
